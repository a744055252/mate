<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cnsmash.mapper.UserRankMapper">

    <select id="pageFighter" resultType="com.cnsmash.pojo.vo.UserDetail">
        SELECT DISTINCT u.id, u.*, f.id as head, f.src as headSrc
        FROM user_rank ur
        INNER JOIN user u on u.id = ur.user_id
        INNER JOIN upload_file f on u.head = f.id
        <if test="ro.fighterNo != null and ro.fighterNo != ''">
            INNER JOIN user_fighter uf on uf.user_id = ur.user_id
                  AND uf.fighter_no = #{ro.fighterNo} AND uf.total &gt;= 5
        </if>
        <where>
            <if test="ro.fighterNo != null and ro.fighterNo != ''">
                uf.id IS NOT NULL
            </if>
            and ur.quarter = #{ro.quarter}
        </where>
        ORDER BY ur.rank
    </select>
</mapper>